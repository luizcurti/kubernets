# IMPORTANTE: Nunca versionar esse arquivo com senhas reais.
# Em produção, use Sealed Secrets ou External Secrets Operator.
# Os valores abaixo são apenas para ambiente de desenvolvimento local.
#
# Para gerar um valor base64: echo -n "sua-senha-forte" | base64
apiVersion: v1
kind: Secret
metadata:
  name: mysql-secret
type: Opaque
data:
  # "changeme-dev-only" em base64 — TROQUE ANTES DE USAR EM PRODUÇÃO
  root-password: Y2hhbmdlbWUtZGV2LW9ubHk=
